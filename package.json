{
  "name": "traz-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Traz AI Assistant — server-side proxy for AI services that need a hidden key (Tavily search now, more later).",
  "scripts": {
    "dev": "vercel dev"
  }
}
